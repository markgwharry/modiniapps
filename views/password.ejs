<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update password · Modini Apps</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <style>
      .card {
        max-width: 640px;
        margin: 2rem auto;
      }
      .form-group {
        margin-bottom: 1rem;
      }
      .form-group label {
        display: block;
        font-weight: 600;
        margin-bottom: 0.25rem;
      }
      .form-group input {
        width: 100%;
        padding: 0.75rem;
        border-radius: 6px;
        border: 1px solid rgba(0, 0, 0, 0.2);
        font-size: 1rem;
      }
      .form-errors,
      .form-success {
        margin: 1rem 0;
        padding: 1rem;
        border-radius: 8px;
      }
      .form-errors {
        background: #fef0f0;
        color: #7a0611;
      }
      .form-success {
        background: #e6f4ea;
        color: #0f5132;
      }
      .form-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .password-helper {
        font-size: 0.85rem;
        color: rgba(0, 0, 0, 0.6);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card">
        <header class="header">
          <div>
            <h1>Change your password</h1>
            <p>Use a strong password that you don’t reuse elsewhere.</p>
          </div>
          <div class="actions">
            <a href="/profile" class="button secondary">← Back to profile</a>
          </div>
        </header>

        <% if (errors && errors.length) { %>
          <div class="form-errors" data-server-errors>
            <ul>
              <% errors.forEach(error => { %>
                <li><%= error %></li>
              <% }) %>
            </ul>
          </div>
        <% } %>

        <% if (success) { %>
          <div class="form-success" data-server-success><%= success %></div>
        <% } %>

        <form method="POST" action="/profile/password" data-password-form>
          <div class="form-group">
            <label for="currentPassword">Current password</label>
            <input id="currentPassword" name="currentPassword" type="password" required autocomplete="current-password" />
          </div>

          <div class="form-group">
            <label for="newPassword">New password</label>
            <input id="newPassword" name="newPassword" type="password" required autocomplete="new-password" />
            <p class="password-helper">Must be at least 8 characters and different from your current password.</p>
          </div>

          <div class="form-group">
            <label for="confirmPassword">Confirm new password</label>
            <input id="confirmPassword" name="confirmPassword" type="password" required autocomplete="new-password" />
          </div>

          <div class="form-errors" data-runtime-errors hidden></div>

          <div class="form-actions">
            <a href="/" class="secondary-link">← Dashboard</a>
            <button class="button" type="submit">Update password</button>
          </div>
        </form>
      </div>
    </div>

    <script src="/js/account.js" defer></script>
  </body>
</html>
